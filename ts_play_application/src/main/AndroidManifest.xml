<?xml version="1.0" encoding="utf-8"?>
<manifest package="pr.tongson"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".mvp.TongsonApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".mvp.register.RegisterActivity">
        </activity>

        <receiver
            android:name=".broadcastreceiver.MyReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TONGSONBS"/>
            </intent-filter>
        </receiver>

        <service
            android:name=".service.MyService"
            android:enabled="true"
            android:exported="true">
        </service>
    </application>

</manifest>